# Rails secret key base (generate with: ruby -rsecurerandom -e 'puts SecureRandom.hex(64)')
SECRET_KEY_BASE=

# Active Record Encryption (generate with: bin/rails db:encryption:init)
ACTIVE_RECORD_ENCRYPTION_PRIMARY_KEY=
ACTIVE_RECORD_ENCRYPTION_DETERMINISTIC_KEY=
ACTIVE_RECORD_ENCRYPTION_KEY_DERIVATION_SALT=

# Gmail IMAP/SMTP
GMAIL_USERNAME=your-email@gmail.com
GMAIL_PASSWORD=your-app-password
GMAIL_IMAP_HOST=imap.gmail.com
GMAIL_SMTP_HOST=smtp.gmail.com

# Action Mailbox (Rails expects this exact env var name)
RAILS_INBOUND_EMAIL_PASSWORD=your-ingress-password

# OpenRouter API (for LLM analysis)
OPENROUTER_API_KEY=your-openrouter-key

# VirusTotal API
VIRUSTOTAL_API_KEY=your-virustotal-key

# WhoisXML API
WHOISXML_API_KEY=your-whoisxml-key

# Brave Search API (for entity verification OSINT)
BRAVE_SEARCH_API_KEY=your-brave-search-key
BRAVE_SEARCH_RATE_LIMIT=1  # requests per second (free=1, paid=15 or 20)

# Community Threat Intelligence Reporting (all optional)
THREATFOX_AUTH_KEY=
ABUSEIPDB_API_KEY=
SPAMCOP_SUBMISSION_ADDRESS=

# Per-sender rate limiting (submissions per hour, 0 = disabled)
MAX_SUBMISSIONS_PER_HOUR=20

# Admin email (receives commands, manages allowed senders)
ADMIN_EMAIL=admin@example.com

# Trusted domains (emails from these domains get contact triage instead of full fraud analysis)
TRUSTED_DOMAINS=

# App host (for mail_fetcher relay)
APP_HOST=http://localhost:3000
